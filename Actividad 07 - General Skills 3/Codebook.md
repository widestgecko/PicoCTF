# Descripción.

Run the Python script `code.py` in the same directory as `codebook.txt`. **Pistas:** On the webshell, use `ls` to see if both files are in the directory you are in. The `str_xor` function does not need to be reverse engineered for this challenge.

# Solución.

1. Descargue los dos archivos del reto en la misma carpeta.
2. Ejecute el archivo .python. **Flag: picoCTF{c0d3b00k_455157_197a982c}**